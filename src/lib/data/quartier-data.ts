/**
 * STUB — French neighborhood (quartier) data removed.
 * TODO: Remove all usages and delete this file.
 */

export interface QuartierProfile {
  name: string
  slug: string
}

export function getQuartierData(_citySlug: string, _quartierSlug: string): QuartierProfile | null {
  return null
}
