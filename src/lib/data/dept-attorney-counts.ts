/**
 * STUB — French department artisan counts removed.
 * TODO: Replace with US state attorney counts from database, then delete.
 */

export const DEPT_ARTISAN_COUNTS: Record<string, { artisans: number; btp: number }> = {}

export function getDeptArtisanCounts(_deptCode: string): { artisans: number; btp: number } | null {
  return null
}
